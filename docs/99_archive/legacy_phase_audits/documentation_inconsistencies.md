# Documentation & Material Inconsistencies Audit
**LPDG Innovation Hub Selection Challenge 2026**  
*Cross-Document Discrepancies, Format Clashes, and Unresolved Nuances (Phase 11)*

---

## Comparative Discrepancy Matrix

| Item / Dimension | Document / File A | Document / File B | Nature of Discrepancy | Operational Impact & Mitigation | Should We Ask LPDG? |
|---|---|---|---|---|---|
| **1. Gateway Fleet Count** | **Challenge Brief p. 2**: *"About 320 gateways are installed on rooftops, in basements..."* | **Data Dictionary p. 1 & `gateway_master.csv`**: Exactly 332 rows; Telemetry has 280 (Aug) to 308 (Mar), 321 unique overall. | Minor approximate description vs exact database inventory. Master includes 12 decommissioned units and 11 future units. | Systems must filter by active dates (`installed_on` vs `decommissioned_on`) rather than assuming static 320. | No (Observed in data). |
| **2. Gateway ID String Formatting** | **Data Dictionary & Raw Data**: 17-character colon-delimited hex (e.g. `02:14:06:F5:63:96`). | **`baseline_3sigma.py` Output**: 12-character bare hex (e.g. `021406F56396`). | Syntactic discrepancy between data representation and baseline script output. | `validate_submission.py` explicitly normalizes both formats via regex. | No (Confirmed by validator code). |
| **3. File Encoding of CSV Assets** | **Data Dictionary p. 1**: Recommends standard `pd.read_csv()`. | **Actual CSV Files**: Encoded in `latin1` / `cp1252` with German characters (`ß`, `ä`, `ö`, `ü`). | Default `pd.read_csv()` in Python 3 on Linux/macOS/Windows crashes with `UnicodeDecodeError` on byte `0xdf`. | Pipelines must explicitly pass `encoding="latin1"` or `encoding="cp1252"`. | No (Standard data engineering fix). |
| **4. Meter Read Horizon Omission** | **Data Dictionary p. 1, 5**: Describes `meter_read_success.csv` as weekly read counts without noting cutoff. | **Actual `meter_read_success.csv`**: Contains data only up to `2026-01-26` (omits entire Feb–Mar scoring period). | The documentation does not explicitly state that meter read success is withheld during the scoring window. | Reflects real-world business lag: during live operations, meter reading records are delayed. | No (Intentional temporal structure). |
| **5. Exact Cost Evaluation Formula** | **Challenge Brief p. 2–3**: States €380 per visit, €600 per week for unattended broken gateways. | **Scoring Script**: Undisclosed internal script; no programmatic formula provided to students. | Ambiguity on whether a visit to a failing gateway incurs €380 + €0 ongoing penalty, or how multi-week persistent failures are resolved if visited in week $k$. | Crucial for Data Science & ML tracks attempting to optimize decision thresholds against the true evaluation metric. | **YES (High Priority Question Candidate)** |
| **6. Operational Role of `engineer_review`** | **Data Dictionary p. 5**: Single snapshot dated `2026-02-15` for 120 gateways (60 Normal, 60 Schlecht). | **Challenge Brief**: No mention of how `engineer_review` relates to scoring or training. | Unclear if this is an offline validation ground truth, an operational signal received during Week 3, or part of live session assessment. | Affects ML/DS tracks on whether this file should be withheld until Week 3 to prevent temporal leakage. | **YES (High Priority Question Candidate)** |
| **7. Submission Archive Folder Structure** | **README.txt & Scripts**: Expects data directly under `./data/`. | **`03-challenge-data.zip`**: Extracts to `03-challenge-data/data/`. | Extraction creates a nested folder `03-challenge-data/data/` if unzipped naively. | Readme warns: *"You will get a folder called 'data'. Keep that name - the brief and both scripts expect it."* | No (Clear in README). |
