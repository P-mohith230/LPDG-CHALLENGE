# LPDG 2026 — Official Screen Recording Script (6–8 Minutes)

**Target Duration**: Exactly 7 minutes (Permitted window: 6:00 to 8:00)  
**Presenter**: Candidate `23091a3286`  
**Recording Mode**: Screen capture with clear microphone voiceover  

---

### Timing Breakdown & Cue Sheet

| Timestamp | Section | Visual Focus | Spoken Script & Key Talking Points |
| :--- | :--- | :--- | :--- |
| **0:00 – 0:45** | **Problem & Operational Objective** | Show `README.md` title and Executive Summary diagram. | *"Welcome to the LPDG Innovation Hub Selection Challenge 2026 presentation for Candidate 23091a3286. Our operational mission is to optimize weekly technician dispatches across 320 smart-meter cellular gateways over 8 scored weeks, exactly 15 visits per week. The economic objective is minimizing total utility cost: €380 per dispatch plus €600 per week for unaddressed collection outages. Under the unified 16-week benchmark, the unsupervised 3-Sigma baseline incurs €164,400. Our validated champion pipeline reduces this to €115,200, delivering a 29.9% cost reduction."* |
| **0:45 – 1:30** | **Repository Architecture & Layout** | Show terminal directory tree (`ls -la` or IDE sidebar). | *"Let's examine the clean, modular repository layout. In the root, we have our single-command runners `run.py` and `run.sh`, `requirements.txt`, `DECISIONS.md`, `AI-USAGE.md`, and the official output `predictions.csv`. In `src/`, the codebase is decoupled into `data/` for partition loading, `features/` for leakage-safe matrix construction, `models/` for target definition and HistGradientBoosting training, `intelligence/` for operational failure signatures and priority ranking, and `evaluation/` for the 16-week economic cost simulator. Everything is verified by 70 automated tests in `tests/`."* |
| **1:30 – 2:15** | **Data Ingestion & Temporal Leakage Firewall** | Open `src/features/builder.py` and `src/models/target.py` showing `validate_target_leakage_firewall`. | *"Data hygiene and leakage prevention are paramount. Every weekly decision boundary is set strictly to Monday 00:00:00 UTC. The feature builder enforces an exact temporal firewall: all telemetry timestamps must satisfy $t < T$, while the forward target is evaluated strictly in $[T, T + 7\text{d})$. We discovered and fixed critical data nuances: first, `offline_duration_sec` was found to be a frozen register snapshot, so we compute peak event duration bounded strictly by 168 hours; second, we enforce the physical hourly conservation law where observed plus missing hours equals exactly 168.0."* |
| **2:15 – 3:15** | **Selected Part 2 Track: Machine Learning** | Open `DECISIONS.md` Decision 5 and `EXPERIMENT_RESULTS.md`. | *"For Part 2, we specialized in Track E: Machine Learning. The primary goal of Track E is strictly defeating `baseline_3sigma.py` under the asymmetric €380 vs €600 cost matrix. We demonstrate this across three pillars: first, beating the baseline by €49,200 (29.9% cost reduction) and cutting unaddressed fault penalties by 67.2%; second, feature ablation proving that removing silence features incurs a €5,400 penalty; and third, dual adversarial validation: evaluating temporal walk-forward generalization across quarterly drift (PR-AUC 0.8465) and 5-fold device-disjoint validation across completely unseen gateways (PR-AUC 0.7658)."* |
| **3:15 – 4:15** | **The Five Modular Innovations Evaluated** | Open `docs/innovation/07_final_innovation_decision.md`. | *"In the Innovation Phase, we investigated 5 modular candidate technologies: Innovation 1 tracks temporal deterioration trends over 7d vs 21d windows, cutting temporal variance by 60%. Innovation 2 implements 5 domain failure signatures with relative risks up to 20.6x. Innovation 3 builds gateway-specific 28-day self-baselines with a 72-hour cold start guard. Innovation 4 decouples risk from deterioration via a priority engine. Crucially, Innovation 5 evaluated an unsupervised Isolation Forest novelty detector. Our forensic audit proved it primarily flagged benign telemetry missingness and high-gain Yagi antennas, increasing cost by €3,600. Based on evidence, Innovation 5 was formally rejected from primary dispatch."* |
| **4:15 – 5:15** | **C3 vs C10 Architecture Resolution** | Show the C0–C10 comparison table in `docs/innovation/FINAL_INNOVATION_AUDIT.md`. | *"We evaluated 11 combination architectures (C0 through C10). The key decision was resolving between C3 and C10. Under our 3-tier hierarchy: First, Primary Cost: C3 achieves €115,200 vs €117,600 for C10, saving €2,400 more and leaving 4 fewer unaddressed faults (40 vs 44). Second, Generalization Stability: C3 achieves higher unseen-gateway PR-AUC (0.7658 vs 0.7438) and 42% lower fold standard deviation (€1,489 vs €2,590). Third, Complexity: C3 is leaner with 32 features and pure calibrated probabilities. Therefore, C3 was promoted as our Champion Dispatch Pipeline, while Failure Signatures and Deterioration serve as our primary diagnostic explanation layer."* |
| **5:15 – 6:15** | **Live Execution: One-Command Pipeline** | Open terminal, run `python run.py`. Show execution logs. | *"Now let's demonstrate single-command reproducibility live. I execute `python run.py`. The script loads the raw telemetry, constructs the 32-feature matrix with personal gateway baselines, fits HistGradientBoosting, evaluates weekly risk probabilities across the 8 scored weeks, enforces the 2-week cooldown policy, and deterministically ranks the top 15 gateways per week. As you see, it completes in seconds with zero manual intervention, zero internet dependency, and zero runtime API keys."* |
| **6:15 – 6:45** | **Output Verification: predictions.csv** | Open `predictions.csv` in terminal / text viewer (`head -n 20 predictions.csv`). | *"Here is the generated `predictions.csv`. Notice the strict schema compliance: exactly 120 rows, 8 scored weeks from 2026-02-02 to 2026-03-23, exactly 15 gateways per week, ranks 1 to 15, valid 12-character hex gateway IDs, calibrated continuous risk scores, and rich, evidence-grounded diagnostic explanation strings that incorporate our domain failure signatures, strictly bounded under the 300-character maximum limit."* |
| **6:45 – 7:15** | **Official Grader Validation** | Run `python validate_submission.py predictions.csv`. | *"Next, we run the official LPDG validator script provided by the challenge organizers. As shown on screen, the validator outputs `predictions.csv: OK` and exits with code 0. Every single validation constraint—row count, week boundaries, rank continuity, ID format, score range, and reason length—passes without error."* |
| **7:15 – 7:45** | **Automated Test Suite & Determinism** | Run `python -m unittest discover -s tests -p "test_*.py" -v`. | *"To guarantee production robustness, we run our full automated test suite. All 70 unit tests pass with zero failures and zero errors in approximately 64 seconds. This covers hourly grid construction, physical bounding invariants, leakage firewalls, economic simulator accounting, and clean-machine byte-for-byte SHA256 reproducibility."* |
| **7:45 – 8:00** | **Limitations & Conclusion** | Return to `README.md` or `DECISIONS.md`. | *"In conclusion, our solution provides an evidence-based, 29.9% cost reduction with transparent, auditable decision intelligence. We acknowledge our documented limitations: sub-weekly transient micro-outages are not resolved by weekly snapshots, and cooldown windows are currently static. If granted two additional weeks, we would implement dynamic signature-based cooldowns and survival analysis. Thank you for your review."* |

---

### Technical Setup Checklist for Recording
1. Set display resolution to 1080p (1920x1080) for clear text readability.
2. Open terminal in repository root.
3. Have VSCode or IDE open with `README.md`, `DECISIONS.md`, and `docs/innovation/FINAL_INNOVATION_AUDIT.md`.
4. Keep terminal commands ready in clipboard or shell history:
   - `python run.py`
   - `python validate_submission.py predictions.csv`
   - `python -m unittest discover -s tests -p "test_*.py" -v`
5. Keep track of the timer to finish smoothly between 6:30 and 7:30.
